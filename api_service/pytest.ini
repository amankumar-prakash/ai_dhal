[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
markers =
    smoke: Fast sanity checks covering the critical happy-paths (< 10 s total)
    unit: Pure unit tests — no HTTP, no I/O
    integration: Tests that require live external services (Supabase, workers)
    authz: Authorization and RBAC boundary tests
