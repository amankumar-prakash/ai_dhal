# Orchestrator — decide (optional alternate user template)

## System

Same role as orchestrator/system.md. Prefer that file for full system text.

## User

Phases completed: {phases_completed}
Last phase rollup:
{last_phase_rollup}

Decide `run_phase`, `loop_phase`, `finish_job`, or `abort_guardrail`.
